README

Base script - logGenShip.py

Use python3 

In order to activate these scripts, you need to use python3 and the log shipping page under libraries >> HTTP Bulk

To change the random range of log lines edit:

x = randint(200,400)
